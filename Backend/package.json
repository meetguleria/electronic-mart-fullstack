{
  "name": "electronic_mart_backend",
  "version": "1.0.0",
  "description": "Node.js API connected to Postgres DB",
  "main": "server.js",
  "scripts": {
    "db:migrate:test": "sequelize-cli db:migrate --env test",
    "db:seed:test": "sequelize-cli db:seed:all --env test",
    "test": "cross-env NODE_ENV=test jest --runInBand"
  },
  "author": "Meet Guleria",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "sequelize-cli": "^6.6.3",
    "supertest": "^7.1.1"
  }
}
